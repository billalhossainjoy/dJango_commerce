[project]
name = "e-commerce-server"
version = "0.1.0"
description = "Django API for the e-commerce application"
requires-python = ">=3.14,<3.15"
dependencies = [
    "Django==6.1",
    "django-environ==0.12.0",
    "djangorestframework==3.18.0",
    "psycopg[binary]==3.3.2",
]

[tool.uv]
package = false
